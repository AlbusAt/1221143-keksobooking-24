/* const userMapFilter = document.querySelector('.map__filters');
const userFilterElement = userMapFilter.querySelectorAll('option');
function onFilterChange (evt) {
  userFilterElement.addEventListener('change', () => {

  });
}
**/
